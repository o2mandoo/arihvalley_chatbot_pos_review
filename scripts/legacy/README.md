# Legacy scripts

This folder keeps original script implementations before the pipeline refactor.

- These files are preserved for reference/history.
- The recommended execution path is `scripts/pipeline/`.
- Use wrappers in `scripts/*.js` only if you need backward compatibility.
